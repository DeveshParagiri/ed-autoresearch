--- /Users/devparagiri/Research/ED/data/ed-source/code/ED_params.defaults.cfg	2022-05-06 11:41:52
+++ configs/ED_params.defaults.cfg	2026-03-20 19:50:40
@@ -63,7 +63,10 @@
                                // numerics, actual site area is read in, is huge,
                                // and can be used for area dependent calcs later
 c2b                  = 2.0;    // Carbon to biomass conversion
-growth_resp          = 0.5;    // Fraction of npp lost as growth respiration, 0.33 in ED 2001
+growth_resp          = 0.33;   // was 0.5; restored to ED 2001 value (Waring et al. 1998).
+                               // 0.33 is the standard literature value for the fraction of
+                               // gross carbon uptake consumed by growth respiration.
+                               // 0.5 was undocumented and gives boreal trees 17% less carbon.
 mass_of_water        = 1.0;    // @TODO roughly- fix!, (kg/mm/m2)
 
 ////////////////////////////////////////
@@ -162,8 +165,14 @@
 c2n_root       = 10.0;
 c2n_stem       = 150.0;
 m1             = 0.2;   // 0.15
-m2             = 10.0;
-m3             = 20.0;  // 10, carbon balance mortality parameter 
+m2             = 2.0;   // was 10.0; reduced to cap maximum carbon-starvation mortality
+                        // at 2 yr^-1 (half-life ~4 months), consistent with observed
+                        // die-off timescales (McDowell et al. 2011 New Phytologist).
+                        // m2=10 implied entire cohort dies 10x/yr, physically unrealistic.
+m3             = 5.0;   // was 20.0; softened sigmoid to allow trees with cbr_bar near 0
+                        // to survive. Trees have stored carbon reserves (sapwood, starch)
+                        // that buffer 2-3 years of negative carbon balance (Hoch et al. 2003).
+                        // m3=20 gave half-life of 50 days at cbr=0; m3=5 gives 4.5 years.
 agf_bs         = 0.8;   // Ratio of above gnd stem to total stem (stem plus structural roots)
 rho_max1       = 0.9;
 rho_max2       = 0.7;   // Eliminate mortality from this source for NA spp 
