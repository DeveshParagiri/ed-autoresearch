# Research memory

Updated: 2026-08-19

`experiment.stock-baseline` is complete. Three runs evaluated the pinned TRENDY v14 EDv3 S3 burned-area output, and the two matched runs at revision `d1518e88f03c371303bd06d9ff767d6ea1ee3358` reproduced the candidate artifact, complete metric vector, ILAMB score products, and five canonical figures byte for byte. The GFED5 overall score is 0.437410 and the GFED4.1s overall score is 0.477503. The stock field underestimates global mean burned area under both products, and its weakest scalar component is spatial distribution, especially against GFED5.

The researcher has confirmed the optimization target: maximize the full-period GFED5 ILAMB Overall score by changing and tuning the ED fire mechanism under the fixed evaluation contract. Direct iteration and Optuna may use trusted GFED5 metrics repeatedly. GFED4.1s reports benchmark sensitivity. Candidate-generation code must not use GFED arrays, fire-presence masks, residuals, or recomputed target means to construct its output.

The project now owns the official ED v3.0 publisher source, dependency sources, input bundle, global 1981-2016 simulation, environment guide, evaluation script, and release archives. `model/stock-ed/UPSTREAM.toml` pins the source provenance and states its limit. The publisher simulation is byte-identical to the existing `data/inputs/ecosystem/ed-simulation.nc`, so the release-layout copy is a relative symlink. Completed stock runs retain `contract.ed-fire.burned-area.v1`. Future candidates use `contract.ed-fire.burned-area.v2`, which leaves the benchmarks, ILAMB metrics, 2001-2016 period, eight reporting regions, and GFED5 ranking rule unchanged while replacing the misleading figure scales with one protected deterministic SciencePlots renderer and bundled Basier Square fonts. GFED5 emissions remain outside this contract because the native baseline has no `fFire` output.

The historical `ed-autoresearch` line has now been recovered under `model/other-models/`. The complete Git bundle contains all 115 commits reachable from remote `main`, `coupled-refit-gfed5`, and `modelD-paper-params`. A checked materialized archive exposes all 406 committed versions across the 381 paths in the model-bearing directories, including overwritten A, B, C, paper-D, combustion, coupled, human-driver, G, H, and I artifacts. The branch-tip view separately retains 303 parameter JSON files, a concise final-model registry, the exact one-degree GDP grid, a replay script, and a fixed historical figure script. Models B, legacy C, and coupled Models C-I reproduce under their archived protocols. Model A is partial because its exact frozen LAI artifact was never committed. Model H has a documented platform-sensitive seasonal phase-tie drift of 0.001111, while its other components reproduce. No historical model-output NetCDF was committed. Benchmark-derived output masks and recomputed target normalization keep the recovered outputs outside the clean line. Parameter optimization against GFED scores remains allowed.

The active blocker is still source-level control, not missing code or data. The official archive has no Git revision, and no recovered record proves which source, build, input selection, and command produced the separate TRENDY v14 artifact. The immediate action is to declare a stock-source reproduction experiment, build and run the publisher release in a controlled environment, record the exact command and outputs, and compare them with both the publisher simulation and pinned TRENDY baseline. Do not modify the fire mechanism or introduce Optuna until that direct control is understood and reproducible.
