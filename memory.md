# Research memory

Updated: 2026-08-19

`experiment.stock-baseline` is complete. Three runs evaluated the pinned TRENDY v14 EDv3 S3 burned-area output, and the two matched runs at revision `d1518e88f03c371303bd06d9ff767d6ea1ee3358` reproduced the candidate artifact, complete metric vector, ILAMB score products, and five canonical figures byte for byte. The displayed GFED5 Overall score is 0.437 and the GFED4.1s Overall score is 0.478; the run evidence retains full precision. The stock field underestimates global mean burned area under both products, and its weakest scalar component is spatial distribution, especially against GFED5.

The researcher has confirmed the optimization target: maximize the full-period GFED5 ILAMB Overall score by changing and tuning the ED fire mechanism under the fixed evaluation contract. Direct iteration and Optuna may use trusted GFED5 metrics repeatedly. GFED4.1s reports benchmark sensitivity. Candidate-generation code must not use GFED arrays, fire-presence masks, residuals, or recomputed target means to construct its output.

The project now owns the official ED v3.0 publisher source, dependency sources, input bundle, global 1981-2016 simulation, environment guide, evaluation script, and release archives. `model/stock-ed/UPSTREAM.toml` pins the source provenance and states its limit. The publisher simulation is byte-identical to the existing `data/inputs/ecosystem/ed-simulation.nc`, so the release-layout copy is a relative symlink. Completed stock runs retain `contract.ed-fire.burned-area.v1`. Future candidates use `contract.ed-fire.burned-area.v2`, which leaves the benchmarks, ILAMB metrics, 2001-2016 period, eight reporting regions, and GFED5 ranking rule unchanged while replacing the misleading figure scales with one protected deterministic SciencePlots renderer and bundled Basier Square fonts. GFED5 emissions remain outside this contract because the native baseline has no `fFire` output.

`experiment.models-a-i-reproduction` is complete. The [selected reproduction](research/experiments/experiment.models-a-i-reproduction/runs/run.20260819T113431Z.96777d94/run.json) regenerated all thirteen retained variants, passed all thirteen replay checks, evaluated each output against GFED5 and GFED4.1s, and retained 78 standardized per-model figures plus two overviews. Model G7 has the highest displayed GFED5 Overall score in this set at 0.686. Model A remains a partial source reconstruction because its exact frozen LAI artifact was never committed. Model F remains reproducible but noncomparable as a clean candidate. Model H exactly reproduced its archived values in the pinned run, although the earlier platform-sensitive phase-tie warning remains relevant. `Ibest` is the archived hybrid's name, not a claim that it wins this standardized comparison.

The Models A-I record lives directly under `model/`. The complete Git bundle contains all 115 commits reachable from remote `main`, `coupled-refit-gfed5`, and `modelD-paper-params`. Its checked commit record exposes all 406 committed versions across the 381 model-bearing paths, while the branch-tip material retains 303 parameter JSON files, the exact one-degree GDP grid, the model registry, replay code, and figure tooling. No prior model-output NetCDF was committed; outputs are regenerated by the recorded experiment. Benchmark-derived masks and target normalization in the recovered generators keep those outputs outside the current clean-candidate contract, but their mechanisms and parameters may seed a new declared experiment after that construction is removed. Parameter optimization against the trusted GFED5 score remains allowed.

The active blocker is now source-level control, not missing A-I evidence, code, or data. The official ED archive has no Git revision, and no recovered record proves which source, build, input selection, and command produced the separate TRENDY v14 artifact. The next experiment should build and run the publisher release in a controlled environment, record the exact command and outputs, and compare them with both the publisher simulation and pinned TRENDY baseline. After that direct control is understood, a mechanism experiment can extend the relevant A-I work and may use Optuna to maximize the trusted GFED5 Overall score under the fixed contract.
