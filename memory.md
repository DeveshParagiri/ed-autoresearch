# Research memory

Updated: 2026-08-18

The current frontier is `experiment.stock-baseline`. Its candidate is the pinned TRENDY v14 EDv3 S3 native burned-area output, not a reconstruction of native ED from source. The source revision and producing command are absent from this workspace, so the run can establish a reproducible evaluation floor but cannot claim source-level reproduction.

All declared data resolve. The active burned-area contract locks the native ED output, GFED5, GFED4.1s, both ILAMB configurations, ILAMB 2.7.3, the 2001-2016 period, eight reporting regions, metric fields, plot scales, and five canonical figures. GFED5 emissions are installed but remain outside this contract because the native baseline has no `fFire` output.

The immediate action is to complete and record the stock baseline. After that, inspect its metrics and figures, rerun it to establish deterministic evaluation, and open the first mechanistic experiment. Do not introduce Optuna until a direct candidate implementation and development objective can be reproduced without search.

Model A through E parameters, outputs, masks, and expected scores remain excluded from the clean research line.
