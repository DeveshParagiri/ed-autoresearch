# Persistent-lightning overlap audit at `121c83c`

The reverse evaluator gives the proposed direction independently in every spatial fold. `log_lightning` has effects `-0.012145,-0.013313,-0.022793,-0.025164`, while causal `log_lightning_ema24` has effects `+0.000635,+0.049159,+0.015082,+0.009645`. The slow-memory effect is weakest but still positive in fold 0. Causal `log_lightning_ema12` is also positive in all four folds, and the `log_lightning_ema12*log_lightning_ema3` interaction is negative in all four. These are reverse-evaluator directions, not fitted coefficients for the proposed law.

The pinned model already has several lightning terms, but none duplicates the proposed state contrast. `_ignition_combustibility_arrival_order` converts current lightning through `L/(L+0.02)`, compares one-month ignition/combustion arrival order, smooths that order over 12 months, and normalises its factor by a trailing mean (`model.py` at pinned lines 702-861). `_rare_lightning_ignition` uses the same current half-saturation as an additive rare source (`973-1114`). Its onset term then rewards a positive current departure from EMA3 and multiplies it by saturated EMA3 lightning (`1116-1178`), so it explicitly favours fresh flash pulses. `_annual_regime_closure` uses 12-month lightning variability and EMA12 lightning, but its only EMA24 state is temperature (`1880-2040`). Other incumbent lightning pathways do not form a signed, saturated EMA24-versus-current/EMA3 supply contrast. The proposed natural/open and primary gates overlap existing fuel-support gates intentionally; they prevent a lightning-only multiplier from creating fire on unsupported land, while the new information is entirely in the 24-month contrast.

The fixed pointwise candidate is

`S(L)=L/(L+0.02)`, `C=clip(S(EMA24(L))-0.5*(S(L)+S(EMA3(L))),-1,1)`, and `H'=H*exp(a*G*C)`,

where `H=-log(1-B)` is incumbent fire hazard. A positive contrast raises hazard when persistent background lightning supply exceeds the fast flash state, and a current or recent spike lowers it. The two predeclared support gates are `G_open=natural_fraction*8/(natural_canopy_height+8)` and `G_primary=luh2_primary_fraction`. Both are local, current pointwise fuel-support fractions. The strengths are the fixed weak brackets `a=0.025,0.05,0.10,0.20`, representing at most about 2.5%, 5%, 10%, or 22% hazard response before the support gate. There are no coordinates, neighbours, geography, target-derived values, learned coefficients, or non-input state.

All eight brackets will be screened on the standard four disjoint 15-degree whole-land-cell folds with held annual-log, normalised-allocation, and raw-cycle losses. Exact evaluation is permitted when annual, allocation, and raw-cycle gains are each positive in at least three of four folds and the sum of gains is positive for each metric. This predeclared `3/4 + positive aggregate` rule implements “broadly stable” rather than the earlier all-fold veto. The eligible bracket with the largest sum of foldwise relative gains is taken to the exact evaluator. Final acceptance remains Overall-first: exact Overall must exceed `0.719892388`, prefix causality must be exact, and no ecological ratio may become non-finite, leave `[0.25,4]`, or move by more than 25% relative to the incumbent.
