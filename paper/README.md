# Paper manuscript (Typst)

```bash
cd paper
typst compile paper.typ paper.pdf
```

Requires [Typst](https://typst.app/) (`brew install typst`). Figures are under `figures/`; bibliography is `refs.bib`.

Source: `paper.typ`. Do not regenerate figures from offline replot scripts — the JPGs here are the paper panels.
