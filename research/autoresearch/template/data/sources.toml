schema = "data-sources/v1"

[store]
environment_variable = "__DATA_ROOT_ENV__"
default_relative_to_project = "../data-store"
description = __DATA_STORE_DESCRIPTION_TOML__

sources = []

# Add one [[sources]] record for every catalog ID. Record source_path,
# acquisition, source_name, source_url where available, version, retrieval,
# time_coverage, spatial_coverage, units, preprocessing, limitations, license,
# integrity, and experiments.
